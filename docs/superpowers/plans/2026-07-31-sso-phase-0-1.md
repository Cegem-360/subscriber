# SSO / identity provider — 0. és 1. fázis implementációs terv

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Megszüntetni a regisztrációkori csapat-kötés hibáit (0. fázis), és bevezetni a globális `uuid` azonosítót mind a 17 alkalmazásban úgy, hogy a jelenlegi szinkron viselkedése ne változzon (1. fázis).

**Architecture:** A 0. fázis egy azonnal kiadható hibajavítás a `subscriber` regisztrációjában, plusz egy felmérő eszköz (`identity:audit`), ami a 16 modul-app és a subscriber közti eltéréseket listázza. Az 1. fázis egy additív `uuid` oszlopot vezet be a `users` és `teams` táblákra minden appban, feltölti a meglévő sorokat, és kiegészíti a szinkron payloadokat — mindezt úgy, hogy a régi kód változatlanul működik tovább. Egyik fázis sem kapcsol át semmit SSO-ra.

**Tech Stack:** PHP 8.3+, Laravel 12, Pest 4, Filament 5, Orchestra Testbench (csomag), Laravel Pint, Rector.

## Global Constraints

- Minden PHP fájl `declare(strict_types=1);` sorral kezdődik (a `<?php` után, üres sorral elválasztva).
- Új osztályok `final`-ok, kivéve ahol a csomag öröklésre szánja őket (`Madbox99\UserTeamSync\Models\Team`).
- Két külön repó érintett:
  - **subscriber:** `/Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1` — composer név `madbox-99/quantum`, névtér `App\`.
  - **csomag:** `/Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid` — composer név `madbox-99/laravel-user-team-sync`, névtér `Madbox99\UserTeamSync\`, GitHub `MadBox-99/laravel-user-team-sync`, jelenlegi verzió `v1.9.0`.
- **A `teams.id` és `users.id` értékéhez sehol nem nyúlunk.** A CRM-ben 25+ tábla FK-zik a `teams.id`-ra, és a Filament tenancy is arra épül.
- **Minden csomag-változtatás visszafelé kompatibilis.** Egy app, ami nem állít be semmi újat, pontosan úgy viselkedik, mint ma. Az `uuid` mezők mindenhol `nullable`-ök és opcionálisak a validációban.
- Artisan parancsok `#[Signature]` és `#[Description]` attribútumokkal készülnek (lásd `app/Console/Commands/GenerateTeamsFromCompanyNamesCommand.php`).
- Commit előtt minden taskban: `vendor/bin/pint --dirty`.
- Subscriber tesztek: `vendor/bin/pest`. A `tests/Pest.php` globálisan bekapcsolja a `RefreshDatabase`-t, a `Http::preventStrayRequests()`-et és a `freezeTime()`-ot — külső HTTP hívást minden tesztben `Http::fake()`-elni kell.
- Csomag tesztek: `vendor/bin/pest`, Testbench, SQLite in-memory. A `tests/Pest.php` biztosítja az `authHeaders()` helpert (`Authorization: Bearer test-api-key`).

## Lokális fejlesztés a csomagon

A subscriber a csomagot GitHubról húzza. Ahhoz, hogy a csomag-változtatásokat a subscriberben ki lehessen próbálni, ideiglenesen path repository kell. **Ezt soha ne commitold.**

```bash
cd /Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1
composer config repositories.local-uts path /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid
composer require madbox-99/laravel-user-team-sync:@dev --no-interaction
# ... tesztelés ...
# visszaállítás commit előtt:
git checkout composer.json composer.lock && composer install
```

## File Structure

### subscriber (`/Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1`)

| Fájl | Felelősség |
|---|---|
| `app/Actions/CreateTeamWithUniqueSlug.php` | **új** — csapat létrehozása garantáltan egyedi sluggal, versenyhelyzet-biztosan |
| `app/Filament/Pages/Auth/Register.php` | **módosul** — a nyers `Str::slug()` helyett az új action |
| `app/Console/Commands/IdentityAuditCommand.php` | **új** — a 16 app lekérdezése és az eltérések jelentése |
| `app/Console/Commands/BackfillIdentityUuidsCommand.php` | **új** — uuid feltöltése a subscriber meglévő sorain |
| `app/Console/Commands/PushIdentityUuidsCommand.php` | **új** — uuid-leképezés kiküldése a modul-appokra |
| `app/Models/User.php` | **módosul** — `uuid` a `#[Fillable]` listába, `creating` hook |
| `app/Models/Team.php` | **módosul** — `creating` hook a uuid generálásához |

A subscriber **nem** kap saját uuid-migrációt: a `UserTeamSyncServiceProvider`
`loadMigrationsFrom()`-ot hív, így a csomag migrációi minden appban — a
subscriberben is — automatikusan lefutnak a `php artisan migrate` során.

### csomag (`/Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid`)

| Fájl | Felelősség |
|---|---|
| `src/Receiver/Http/Controllers/IdentityAuditController.php` | **új** — a receiver teljes identitás-állapotának kiadása |
| `src/Receiver/Http/Controllers/IdentityUuidController.php` | **új** — uuid-leképezés fogadása és beírása |
| `src/Receiver/Http/Requests/StoreIdentityUuidsRequest.php` | **új** — a uuid-leképezés validációja |
| `routes/sync-api.php` | **módosul** — két új route |
| `database/migrations/2026_07_31_000000_add_uuid_to_users_and_teams.php` | **új** — uuid oszlopok a receivereken |
| `src/Models/Team.php` | **módosul** — `uuid` a fillable listába |
| `src/Receiver/Http/Requests/CreateUserRequest.php` | **módosul** — opcionális `uuid` |
| `src/Receiver/Http/Requests/CreateTeamRequest.php` | **módosul** — opcionális `uuid` |
| `src/Receiver/Http/Controllers/UserSyncController.php` | **módosul** — a kapott `uuid` eltárolása |
| `src/Receiver/Http/Controllers/TeamSyncController.php` | **módosul** — a kapott `uuid` eltárolása |
| `src/Publisher/Jobs/CreateUserJob.php` | **módosul** — `uuid` a payloadba |
| `src/Publisher/Jobs/CreateTeamJob.php` | **módosul** — `uuid` a payloadba |
| `src/Publisher/PublisherService.php` | **módosul** — `uuid` paraméter átvezetése |

---

# 0. FÁZIS

## Task 1: Egyedi csapat-slug a regisztrációnál

A `company_name` mezőn már van egy form-szintű validációs szabály
(`Register::getCompanyFieldset()`), ami elutasítja a regisztrációt, ha a
cégnévből képzett slug már létezik. A normál úton tehát nem keletkezik ütközés.

**Marad viszont egy versenyhelyzet:** két egyidejű regisztráció mindkettője
átmehet a validáción, mielőtt bármelyik beszúrná a csapatot — ekkor a második
`Team::create()` a unique constrainten hasal el. A user sora ilyenkor már
létrejött, de a csapat és a teljes cross-app provisioning elmarad.

Ez a task **csak ezt a versenyhelyzetet zárja le.** A validációs szabály és az
azt rögzítő teszt (`tests/Feature/RegistrationTest.php` — "rejects registration
when the company name maps to an existing team slug") **változatlanul marad** —
ez szándékos, tesztelt üzleti viselkedés. Ne töröld és ne írd át egyiket sem.

A csapat-létrehozás egy actionbe kerül, ami unique-hibára a következő
sorszámozott slugra lép tovább. A suffixelés így kizárólag verseny-helyreállítási
út, nem a normál működés része.

**Files:**
- Create: `app/Actions/CreateTeamWithUniqueSlug.php`
- Modify: `app/Filament/Pages/Auth/Register.php:44-70`
- Test: `tests/Feature/Actions/CreateTeamWithUniqueSlugTest.php`

**Interfaces:**
- Consumes: `App\Models\Team`
- Produces: `App\Actions\CreateTeamWithUniqueSlug::handle(string $name): \App\Models\Team` — mindig **új** Team sort hoz létre, egyedi sluggal.

- [ ] **Step 1: Írd meg a bukó tesztet az actionre**

Hozd létre: `tests/Feature/Actions/CreateTeamWithUniqueSlugTest.php`

```php
<?php

declare(strict_types=1);

use App\Actions\CreateTeamWithUniqueSlug;
use App\Models\Team;

it('slugifies the name when no team exists yet', function (): void {
    $team = app(CreateTeamWithUniqueSlug::class)->handle('Acme Kft.');

    expect($team->slug)->toBe('acme-kft')
        ->and($team->name)->toBe('Acme Kft.');
});

it('gives a second team with the same name a suffixed slug', function (): void {
    $first = app(CreateTeamWithUniqueSlug::class)->handle('Acme Kft.');
    $second = app(CreateTeamWithUniqueSlug::class)->handle('Acme Kft.');

    expect($first->slug)->toBe('acme-kft')
        ->and($second->slug)->toBe('acme-kft-2')
        ->and($second->id)->not->toBe($first->id);
});

it('keeps counting up past an existing suffixed slug', function (): void {
    Team::query()->create(['name' => 'Acme Kft.', 'slug' => 'acme-kft']);
    Team::query()->create(['name' => 'Acme Kft.', 'slug' => 'acme-kft-2']);

    $team = app(CreateTeamWithUniqueSlug::class)->handle('Acme Kft.');

    expect($team->slug)->toBe('acme-kft-3');
});

it('falls back to a generic base when the name slugifies to nothing', function (): void {
    $team = app(CreateTeamWithUniqueSlug::class)->handle('!!!');

    expect($team->slug)->toBe('team');
});
```

- [ ] **Step 2: Futtasd, győződj meg róla, hogy bukik**

Run: `vendor/bin/pest tests/Feature/Actions/CreateTeamWithUniqueSlugTest.php`
Expected: FAIL — `Target class [App\Actions\CreateTeamWithUniqueSlug] does not exist.`

- [ ] **Step 3: Írd meg az actiont**

Hozd létre: `app/Actions/CreateTeamWithUniqueSlug.php`

```php
<?php

declare(strict_types=1);

namespace App\Actions;

use App\Models\Team;
use Illuminate\Database\UniqueConstraintViolationException;
use Illuminate\Support\Str;

/**
 * Always creates a NEW team with a guaranteed-unique slug.
 *
 * The slug is not an identity key — it only routes Filament tenant URLs — so
 * suffixing it on collision is safe. Joining an existing same-named team is
 * deliberately NOT done: a team carries the subscription and is the tenant
 * boundary, so attaching a stranger's registration to it would leak data.
 */
final class CreateTeamWithUniqueSlug
{
    private const int MAX_ATTEMPTS = 25;

    public function handle(string $name): Team
    {
        $base = Str::slug($name);

        if ($base === '') {
            $base = 'team';
        }

        for ($attempt = 1; $attempt <= self::MAX_ATTEMPTS; $attempt++) {
            $slug = $attempt === 1 ? $base : $base . '-' . $attempt;

            if (Team::query()->where('slug', $slug)->exists()) {
                continue;
            }

            try {
                return Team::query()->create(['name' => $name, 'slug' => $slug]);
            } catch (UniqueConstraintViolationException) {
                // Another request claimed this slug between the check and the
                // insert. Move on to the next suffix.
                continue;
            }
        }

        // Pathological contention or 25+ same-named teams: fall back to a
        // random suffix, which cannot collide in practice.
        return Team::query()->create([
            'name' => $name,
            'slug' => $base . '-' . Str::lower(Str::random(6)),
        ]);
    }
}
```

- [ ] **Step 4: Futtasd a tesztet**

Run: `vendor/bin/pest tests/Feature/Actions/CreateTeamWithUniqueSlugTest.php`
Expected: PASS (4 teszt)

- [ ] **Step 5: Cseréld le a slug-generálást a Register oldalon**

A `app/Filament/Pages/Auth/Register.php` `handleRegistration()` metódusában cseréld ki ezt a blokkot:

```php
        // The team slug is derived from the company name and must be unique
        // (the receiver apps key teams by slug). Persist the team locally so
        // the same slug is reused everywhere and future registrations can be
        // validated against it.
        $teamSlug = Str::slug((string) $user->company_name);

        $team = Team::query()->create([
            'name' => $user->company_name,
            'slug' => $teamSlug,
        ]);

        $user->teams()->attach($team);
```

erre:

```php
        // The team slug must be unique (it is the receiver apps' team key and
        // the Filament tenant route segment). A plain Str::slug() of the
        // company name collides whenever two customers share a name, which
        // aborted registration and skipped cross-app provisioning entirely.
        $team = app(CreateTeamWithUniqueSlug::class)->handle((string) $user->company_name);

        $user->teams()->attach($team);
```

Majd a lentebbi `UserTeamSync::createTeam(...)` hívásban cseréld a `slug: $teamSlug` argumentumot `slug: $team->slug`-ra, és a `teamName: $user->company_name` maradjon változatlan.

Add hozzá a use-t: `use App\Actions\CreateTeamWithUniqueSlug;`
Ha a `Str` importra már nincs szükség a fájlban, töröld a `use Illuminate\Support\Str;` sort.

- [ ] **Step 6: Futtasd a regisztrációs teszteket**

Run: `vendor/bin/pest tests/Feature/RegistrationTest.php`
Expected: PASS, minden teszt

- [ ] **Step 7: Futtasd a teljes tesztkészletet**

Run: `vendor/bin/pest`
Expected: PASS. A 495 meglévő teszt mind zöld marad, plusz az új 4. Külön
ellenőrizd, hogy a "rejects registration when the company name maps to an
existing team slug" teszt továbbra is zöld — ha nem az, hozzányúltál a
validációs szabályhoz, amihez nem szabad.

- [ ] **Step 8: Pint és commit**

```bash
cd /Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1
vendor/bin/pint --dirty
git add app/Actions/CreateTeamWithUniqueSlug.php app/Filament/Pages/Auth/Register.php tests/Feature/Actions/CreateTeamWithUniqueSlugTest.php
git commit -m "fix: a csapat-létrehozás túléli a slug versenyhelyzetet

A company_name validációja kizárja a normál ütközést, de két egyidejű
regisztráció mindkettője átmehet rajta, mielőtt bármelyik beszúrná a
csapatot. A vesztes Team::create() eddig a unique constrainten hasalt el,
és a user csapat, illetve cross-app provisioning nélkül maradt."
```

---

## Task 2: `identity-audit` végpont a csomagban

Ahhoz, hogy fel tudjuk mérni a subscriber és a 16 modul-app közti eltéréseket, a modul-appoknak ki kell adniuk a teljes identitás-állapotukat. Ma nincs ilyen végpont — a `GET /api/user-teams` csak egyetlen felhasználóra válaszol, és a receiver-lokális csapatokat egyáltalán nem mutatja meg.

**Files:**
- Create: `src/Receiver/Http/Controllers/IdentityAuditController.php`
- Modify: `routes/sync-api.php`
- Test: `tests/Feature/Receiver/IdentityAuditControllerTest.php`

**Interfaces:**
- Produces: `GET /api/identity-audit` (Bearer auth), válasz:
  ```
  {
    "teams":       [{"id": int, "name": string, "slug": string}],
    "users":       [{"id": int, "email": string}],
    "memberships": [{"user_email": string, "team_slug": string}],
    "pending_team_attachments": [{"user_email": string, "team_slug": string}]
  }
  ```

- [ ] **Step 1: Írd meg a bukó tesztet**

Hozd létre: `tests/Feature/Receiver/IdentityAuditControllerTest.php`

```php
<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Hash;
use Madbox99\UserTeamSync\Models\PendingTeamAttachment;
use Madbox99\UserTeamSync\Tests\Fixtures\Team;
use Madbox99\UserTeamSync\Tests\Fixtures\User;

it('rejects requests without a valid api key', function (): void {
    $this->getJson('/api/identity-audit')->assertStatus(401);
});

it('returns teams, users, memberships and pending attachments', function (): void {
    $team = Team::create(['name' => 'Acme', 'slug' => 'acme']);
    $orphan = Team::create(['name' => 'Local Only', 'slug' => 'local-only']);

    $user = User::create([
        'name' => 'User',
        'email' => 'user@example.com',
        'password' => Hash::make('pass'),
    ]);
    $user->teams()->attach($team);

    PendingTeamAttachment::create([
        'user_email' => 'ghost@example.com',
        'team_slug' => 'missing-team',
    ]);

    $response = $this->getJson('/api/identity-audit', authHeaders())->assertOk();

    expect($response->json('teams'))->toHaveCount(2)
        ->and(collect($response->json('teams'))->pluck('slug')->all())
            ->toBe(['acme', 'local-only'])
        ->and($response->json('users'))->toBe([
            ['id' => $user->id, 'email' => 'user@example.com'],
        ])
        ->and($response->json('memberships'))->toBe([
            ['user_email' => 'user@example.com', 'team_slug' => 'acme'],
        ])
        ->and($response->json('pending_team_attachments'))->toBe([
            ['user_email' => 'ghost@example.com', 'team_slug' => 'missing-team'],
        ]);

    // The orphan team has no members — this is exactly what the audit must surface.
    expect(collect($response->json('memberships'))->pluck('team_slug'))
        ->not->toContain('local-only');
});
```

- [ ] **Step 2: Futtasd, győződj meg róla, hogy bukik**

Run: `cd /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid && vendor/bin/pest tests/Feature/Receiver/IdentityAuditControllerTest.php`
Expected: FAIL — 404, a route nem létezik

- [ ] **Step 3: Írd meg a controllert**

Hozd létre: `src/Receiver/Http/Controllers/IdentityAuditController.php`

```php
<?php

declare(strict_types=1);

namespace Madbox99\UserTeamSync\Receiver\Http\Controllers;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Http\JsonResponse;
use Illuminate\Routing\Controller;
use Illuminate\Support\Facades\DB;
use Madbox99\UserTeamSync\Models\PendingTeamAttachment;

/**
 * Read-only snapshot of this app's identity state, used by the publisher's
 * `identity:audit` command to diff against its own records before the UUID
 * migration. Introduced because no existing endpoint can reveal teams that
 * exist only on the receiver.
 */
final class IdentityAuditController extends Controller
{
    public function __invoke(): JsonResponse
    {
        /** @var class-string<Model> $teamModel */
        $teamModel = config('user-team-sync.models.team');

        /** @var class-string<Model> $userModel */
        $userModel = config('user-team-sync.models.user');

        return response()->json([
            'teams' => $teamModel::query()
                ->orderBy('id')
                ->get(['id', 'name', 'slug'])
                ->map(fn (Model $team): array => [
                    'id' => $team->getKey(),
                    'name' => $team->getAttribute('name'),
                    'slug' => $team->getAttribute('slug'),
                ])
                ->values(),

            'users' => $userModel::query()
                ->orderBy('id')
                ->get(['id', 'email'])
                ->map(fn (Model $user): array => [
                    'id' => $user->getKey(),
                    'email' => $user->getAttribute('email'),
                ])
                ->values(),

            'memberships' => DB::table('team_user')
                ->join('users', 'users.id', '=', 'team_user.user_id')
                ->join('teams', 'teams.id', '=', 'team_user.team_id')
                ->orderBy('users.email')
                ->orderBy('teams.slug')
                ->get(['users.email as user_email', 'teams.slug as team_slug'])
                ->map(fn (object $row): array => [
                    'user_email' => $row->user_email,
                    'team_slug' => $row->team_slug,
                ])
                ->values(),

            'pending_team_attachments' => PendingTeamAttachment::query()
                ->orderBy('user_email')
                ->get(['user_email', 'team_slug'])
                ->map(fn (PendingTeamAttachment $pending): array => [
                    'user_email' => $pending->user_email,
                    'team_slug' => $pending->team_slug,
                ])
                ->values(),
        ]);
    }
}
```

- [ ] **Step 4: Vedd fel a route-ot**

Módosítsd: `routes/sync-api.php` — add hozzá az importot és a route-ot a group-on belül, a `Route::post('/sync-password', ...)` sor után:

```php
use Madbox99\UserTeamSync\Receiver\Http\Controllers\IdentityAuditController;
```

```php
        Route::get('/identity-audit', IdentityAuditController::class);
```

- [ ] **Step 5: Futtasd a tesztet**

Run: `vendor/bin/pest tests/Feature/Receiver/IdentityAuditControllerTest.php`
Expected: PASS (2 teszt)

- [ ] **Step 6: Futtasd a teljes csomag-tesztkészletet**

Run: `vendor/bin/pest`
Expected: PASS, minden teszt

- [ ] **Step 7: Pint és commit**

```bash
cd /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid
vendor/bin/pint --dirty
git add src/Receiver/Http/Controllers/IdentityAuditController.php routes/sync-api.php tests/Feature/Receiver/IdentityAuditControllerTest.php
git commit -m "feat: identity-audit végpont a receiver állapotának felméréséhez

A publisher identity:audit parancsa ezzel tudja összevetni a saját
nyilvántartását a modul-appokéval. Meglévő végponttal nem lehetett
kideríteni, mely csapatok léteznek csak a receiveren."
```

> **Kiadás — NEM ennek a tasknak a része.** A `v1.10.0` tag, a push és a 16 modul-app deployja a branch mergelése után történik. Az implementer csak commitol. A Task 3 a path repositoryn keresztül látja ezt a végpontot.

---

## Task 3: `identity:audit` parancs a subscriberben

**Files:**
- Create: `app/Console/Commands/IdentityAuditCommand.php`
- Test: `tests/Feature/Console/IdentityAuditCommandTest.php`

**Interfaces:**
- Consumes: `GET /api/identity-audit` (Task 2), `Madbox99\UserTeamSync\Publisher\PublisherService::getActiveApps(): array<string, array{url: string, api_key: ?string, active: bool}>` és `::makeHttpClient(array $app): \Illuminate\Http\Client\PendingRequest`
- Produces: `php artisan identity:audit` — konzolos jelentés + JSON `storage/app/identity-audit-{timestamp}.json`

A jelentés négy eltérés-fajtát mutat appon**ként**:

| Kategória | Jelentés |
|---|---|
| `missing_teams` | a subscriberben létezik, a receiveren nem — a felhasználók ide nem tudnak kötődni |
| `orphan_teams` | csak a receiveren létezik — kézzel felvett vagy elavult csapat |
| `missing_users` | a subscriberben létezik, a receiveren nem — kiesett provisioning |
| `pending_attachments` | felgyűlt, soha fel nem oldható `PendingTeamAttachment` sorok |

- [ ] **Step 1: Írd meg a bukó tesztet**

Hozd létre: `tests/Feature/Console/IdentityAuditCommandTest.php`

```php
<?php

declare(strict_types=1);

use App\Models\Team;
use App\Models\User;
use Illuminate\Support\Facades\Http;
use Madbox99\UserTeamSync\Models\SyncApp;

use function Pest\Laravel\artisan;

beforeEach(function (): void {
    config()->set('user-team-sync.publisher.app_source', 'database');

    SyncApp::create([
        'name' => 'crm',
        'url' => 'https://crm.test',
        'api_key' => 'secret',
        'is_active' => true,
    ]);
});

it('reports a team that exists on the publisher but not on the receiver', function (): void {
    Team::query()->create(['name' => 'Acme', 'slug' => 'acme']);

    Http::fake([
        'https://crm.test/api/identity-audit' => Http::response([
            'teams' => [],
            'users' => [],
            'memberships' => [],
            'pending_team_attachments' => [],
        ]),
    ]);

    artisan('identity:audit')
        ->expectsOutputToContain('crm')
        ->expectsOutputToContain('acme')
        ->assertExitCode(0);
});

it('reports a team that exists only on the receiver', function (): void {
    Http::fake([
        'https://crm.test/api/identity-audit' => Http::response([
            'teams' => [['id' => 3, 'name' => 'Local Only', 'slug' => 'local-only']],
            'users' => [],
            'memberships' => [],
            'pending_team_attachments' => [],
        ]),
    ]);

    artisan('identity:audit')
        ->expectsOutputToContain('local-only')
        ->assertExitCode(0);
});

it('reports a user missing from the receiver', function (): void {
    User::factory()->create(['email' => 'hianyzo@example.com']);

    Http::fake([
        'https://crm.test/api/identity-audit' => Http::response([
            'teams' => [],
            'users' => [],
            'memberships' => [],
            'pending_team_attachments' => [],
        ]),
    ]);

    artisan('identity:audit')
        ->expectsOutputToContain('hianyzo@example.com')
        ->assertExitCode(0);
});

it('survives an app that has not been upgraded yet', function (): void {
    Http::fake([
        'https://crm.test/api/identity-audit' => Http::response('Not Found', 404),
    ]);

    artisan('identity:audit')
        ->expectsOutputToContain('crm')
        ->expectsOutputToContain('identity-audit')
        ->assertExitCode(0);
});

it('writes the full report to storage', function (): void {
    Http::fake([
        'https://crm.test/api/identity-audit' => Http::response([
            'teams' => [],
            'users' => [],
            'memberships' => [],
            'pending_team_attachments' => [],
        ]),
    ]);

    artisan('identity:audit')->assertExitCode(0);

    // freezeTime() in tests/Pest.php makes the filename deterministic, so
    // assert on the exact path rather than globbing — a glob would pick up
    // reports left behind by earlier runs.
    $path = storage_path('app/identity-audit-' . now()->format('Ymd-His') . '.json');

    expect(file_exists($path))->toBeTrue();

    $report = json_decode((string) file_get_contents($path), true);

    expect($report)->toHaveKey('crm');

    unlink($path);
});
```

- [ ] **Step 2: Futtasd, győződj meg róla, hogy bukik**

Run: `cd /Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1 && vendor/bin/pest tests/Feature/Console/IdentityAuditCommandTest.php`
Expected: FAIL — `The command "identity:audit" does not exist.`

- [ ] **Step 3: Írd meg a parancsot**

Hozd létre: `app/Console/Commands/IdentityAuditCommand.php`

```php
<?php

declare(strict_types=1);

namespace App\Console\Commands;

use App\Models\Team;
use App\Models\User;
use Illuminate\Console\Attributes\Description;
use Illuminate\Console\Attributes\Signature;
use Illuminate\Console\Command;
use Illuminate\Support\Facades\File;
use Madbox99\UserTeamSync\Publisher\PublisherService;
use Throwable;

/**
 * Diffs this app's users/teams/memberships against every active module app.
 *
 * Run before the UUID backfill: the backfill pairs records by email and slug,
 * and that pairing is only correct once the divergences this command surfaces
 * have been resolved by hand.
 */
#[Signature('identity:audit {--app= : Limit the audit to a single app key}')]
#[Description('Compare users, teams and memberships against every module app and report divergences.')]
final class IdentityAuditCommand extends Command
{
    public function handle(PublisherService $publisher): int
    {
        $apps = $publisher->getActiveApps();

        if ($only = $this->option('app')) {
            $apps = array_intersect_key($apps, [$only => true]);
        }

        if ($apps === []) {
            $this->warn('No active apps configured. Nothing to audit.');

            return self::SUCCESS;
        }

        $localTeams = Team::query()->pluck('name', 'slug');
        $localUsers = User::query()->pluck('email')->all();
        $localMemberships = $this->localMemberships();

        $report = [];

        foreach ($apps as $appName => $app) {
            $this->newLine();
            $this->info("=== {$appName} ===");

            $remote = $this->fetch($publisher, $appName, $app);

            if ($remote === null) {
                $report[$appName] = ['error' => 'unreachable or /api/identity-audit not deployed'];

                continue;
            }

            $remoteTeamSlugs = collect($remote['teams'] ?? [])->pluck('slug')->all();
            $remoteUserEmails = collect($remote['users'] ?? [])->pluck('email')->all();
            $remoteMemberships = collect($remote['memberships'] ?? [])
                ->map(fn (array $row): string => $row['user_email'] . '|' . $row['team_slug'])
                ->all();

            $entry = [
                'missing_teams' => array_values(array_diff($localTeams->keys()->all(), $remoteTeamSlugs)),
                'orphan_teams' => array_values(array_diff($remoteTeamSlugs, $localTeams->keys()->all())),
                'missing_users' => array_values(array_diff($localUsers, $remoteUserEmails)),
                'missing_memberships' => array_values(array_diff($localMemberships, $remoteMemberships)),
                'pending_attachments' => $remote['pending_team_attachments'] ?? [],
            ];

            $report[$appName] = $entry;

            $this->renderEntry($entry);
        }

        $path = storage_path('app/identity-audit-' . now()->format('Ymd-His') . '.json');
        File::put($path, json_encode($report, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES));

        $this->newLine();
        $this->info("Full report written to: {$path}");
        $this->warn('Resolve every divergence by hand BEFORE running identity:backfill-uuids.');

        return self::SUCCESS;
    }

    /**
     * @return array<int, string> "email|slug" pairs
     */
    private function localMemberships(): array
    {
        return User::query()
            ->with('teams:id,slug')
            ->get(['id', 'email'])
            ->flatMap(fn (User $user): array => $user->teams
                ->map(fn (Team $team): string => $user->email . '|' . $team->slug)
                ->all())
            ->sort()
            ->values()
            ->all();
    }

    /**
     * @param  array{url: string, api_key: ?string, active: bool}  $app
     * @return array<string, mixed>|null
     */
    private function fetch(PublisherService $publisher, string $appName, array $app): ?array
    {
        try {
            $response = $publisher->makeHttpClient($app)->get("{$app['url']}/api/identity-audit");
        } catch (Throwable $throwable) {
            $this->error("  Unreachable: {$throwable->getMessage()}");

            return null;
        }

        if ($response->status() === 404) {
            $this->error('  /api/identity-audit missing — this app still runs an older package version.');

            return null;
        }

        if (! $response->successful()) {
            $this->error("  HTTP {$response->status()} from /api/identity-audit");

            return null;
        }

        return $response->json();
    }

    /**
     * @param  array<string, mixed>  $entry
     */
    private function renderEntry(array $entry): void
    {
        $labels = [
            'missing_teams' => 'Teams missing on the receiver',
            'orphan_teams' => 'Teams that exist ONLY on the receiver',
            'missing_users' => 'Users missing on the receiver',
            'missing_memberships' => 'Memberships missing on the receiver',
        ];

        foreach ($labels as $key => $label) {
            $values = $entry[$key];

            if ($values === []) {
                $this->line("  ✓ {$label}: none");

                continue;
            }

            $this->warn(sprintf('  ! %s: %d', $label, count($values)));

            foreach (array_slice($values, 0, 20) as $value) {
                $this->line("      - {$value}");
            }

            if (count($values) > 20) {
                $this->line(sprintf('      … and %d more (see the JSON report)', count($values) - 20));
            }
        }

        $pending = $entry['pending_attachments'];

        if ($pending !== []) {
            $this->warn(sprintf('  ! Stuck pending attachments: %d', count($pending)));

            foreach (array_slice($pending, 0, 20) as $row) {
                $this->line("      - {$row['user_email']} → {$row['team_slug']}");
            }
        }
    }
}
```

- [ ] **Step 4: Futtasd a tesztet**

Run: `vendor/bin/pest tests/Feature/Console/IdentityAuditCommandTest.php`
Expected: PASS (5 teszt)

- [ ] **Step 5: Futtasd a teljes tesztkészletet**

Run: `vendor/bin/pest`
Expected: PASS

- [ ] **Step 6: Pint és commit**

```bash
cd /Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1
vendor/bin/pint --dirty
git add app/Console/Commands/IdentityAuditCommand.php tests/Feature/Console/IdentityAuditCommandTest.php
git commit -m "feat: identity:audit parancs a modul-appok eltéréseinek felmérésére

A UUID backfill email és slug alapján párosít, ami csak akkor helyes, ha
az itt felszínre hozott eltéréseket előtte kézzel rendezték."
```

> **Éles futtatás — NEM ennek a tasknak a része.** A `php artisan identity:audit` valódi modul-appokat hív hálózaton keresztül; az implementer csak a tesztekig megy. Az éles futtatás a merge után, emberi felügyelettel történik.
>
> **Ez a 0. fázis végpontja.** A jelentést embernek kell átnéznie. Amíg a `missing_teams`, `orphan_teams` és `pending_attachments` sorok nincsenek rendezve, az 1. fázis éles futtatása nem kezdődhet el — a backfill rossz párosítást írna be, amit utána nagyon nehéz visszabontani.

---

# 1. FÁZIS

## Task 4: `uuid` oszlopok a csomag migrációjában

**Files:**
- Create: `database/migrations/2026_07_31_000000_add_uuid_to_users_and_teams.php`
- Modify: `src/Models/Team.php`
- Test: `tests/Feature/Migrations/AddUuidToUsersAndTeamsTest.php`

**Interfaces:**
- Produces: `users.uuid` és `teams.uuid` — `char(36)`, `nullable`, `unique`. A `Madbox99\UserTeamSync\Models\Team::$fillable` kiegészül `'uuid'`-vel.

- [ ] **Step 1: Írd meg a bukó tesztet**

Hozd létre: `tests/Feature/Migrations/AddUuidToUsersAndTeamsTest.php`

```php
<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Schema;
use Illuminate\Support\Str;
use Madbox99\UserTeamSync\Tests\Fixtures\Team;

it('adds a nullable uuid column to users and teams', function (): void {
    expect(Schema::hasColumn('users', 'uuid'))->toBeTrue()
        ->and(Schema::hasColumn('teams', 'uuid'))->toBeTrue();
});

it('allows a team without a uuid', function (): void {
    $team = Team::create(['name' => 'No Uuid', 'slug' => 'no-uuid']);

    expect($team->uuid)->toBeNull();
});

it('accepts a uuid as a fillable attribute', function (): void {
    $uuid = (string) Str::uuid();

    $team = Team::create(['name' => 'With Uuid', 'slug' => 'with-uuid', 'uuid' => $uuid]);

    expect($team->refresh()->uuid)->toBe($uuid);
});

it('rejects a duplicate team uuid', function (): void {
    $uuid = (string) Str::uuid();

    Team::create(['name' => 'First', 'slug' => 'first', 'uuid' => $uuid]);

    expect(fn () => Team::create(['name' => 'Second', 'slug' => 'second', 'uuid' => $uuid]))
        ->toThrow(Illuminate\Database\UniqueConstraintViolationException::class);
});
```

- [ ] **Step 2: Futtasd, győződj meg róla, hogy bukik**

Run: `cd /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid && vendor/bin/pest tests/Feature/Migrations/AddUuidToUsersAndTeamsTest.php`
Expected: FAIL — `hasColumn('users', 'uuid')` false

- [ ] **Step 3: Írd meg a migrációt**

Hozd létre: `database/migrations/2026_07_31_000000_add_uuid_to_users_and_teams.php`

```php
<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * Global, stable identifiers for cross-app identity.
 *
 * Nullable on purpose: existing rows are backfilled by a separate command, and
 * apps that have not been backfilled yet must keep working unchanged. The
 * local `id` columns are untouched — business tables foreign-key to them and
 * Filament tenancy resolves them.
 */
return new class extends Migration
{
    public function up(): void
    {
        if (Schema::hasTable('users') && ! Schema::hasColumn('users', 'uuid')) {
            Schema::table('users', function (Blueprint $blueprint): void {
                $blueprint->char('uuid', 36)->nullable()->unique()->after('id');
            });
        }

        if (Schema::hasTable('teams') && ! Schema::hasColumn('teams', 'uuid')) {
            Schema::table('teams', function (Blueprint $blueprint): void {
                $blueprint->char('uuid', 36)->nullable()->unique()->after('id');
            });
        }
    }

    public function down(): void
    {
        if (Schema::hasColumn('users', 'uuid')) {
            Schema::table('users', function (Blueprint $blueprint): void {
                $blueprint->dropColumn('uuid');
            });
        }

        if (Schema::hasColumn('teams', 'uuid')) {
            Schema::table('teams', function (Blueprint $blueprint): void {
                $blueprint->dropColumn('uuid');
            });
        }
    }
};
```

- [ ] **Step 4: Vedd fel a `uuid`-t a csomag Team modelljébe**

Módosítsd: `src/Models/Team.php` — a `$fillable` tömb legyen:

```php
    protected $fillable = [
        'uuid',
        'name',
        'slug',
    ];
```

- [ ] **Step 5: Futtasd a tesztet**

Run: `vendor/bin/pest tests/Feature/Migrations/AddUuidToUsersAndTeamsTest.php`
Expected: PASS (4 teszt)

- [ ] **Step 6: Futtasd a teljes csomag-tesztkészletet**

Run: `vendor/bin/pest`
Expected: PASS. A migráció additív, semmi meglévő nem törhet el.

- [ ] **Step 7: Pint és commit (tag NÉLKÜL)**

```bash
cd /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid
vendor/bin/pint --dirty
git add database/migrations/2026_07_31_000000_add_uuid_to_users_and_teams.php src/Models/Team.php tests/Feature/Migrations/AddUuidToUsersAndTeamsTest.php
git commit -m "feat: uuid oszlop a users és teams táblákra

Globális, stabil kereszt-app azonosító. Nullable, mert a feltöltés külön
parancsból történik, és a még nem feltöltött appoknak változatlanul
működniük kell. A lokális id oszlopokhoz nem nyúlunk."
```

**Ne adj ki taget.** A Task 6 ugyanebbe a kiadásba tartozik — egy deploy-kör mind a 16 appon, nem kettő.

---

## Task 5: uuid a subscriberben — generálás és backfill

**Files:**
- Create: `app/Console/Commands/BackfillIdentityUuidsCommand.php`
- Modify: `app/Models/User.php` (a `#[Fillable]` attribútum és egy `booted` hook)
- Modify: `app/Models/Team.php` (`booted` hook)
- Test: `tests/Feature/Console/BackfillIdentityUuidsCommandTest.php`

**Interfaces:**
- Consumes: `users.uuid` és `teams.uuid` oszlopok (Task 4)
- Produces: `php artisan identity:backfill-uuids` — minden `uuid IS NULL` sorra `Str::uuid()`-t ír. Idempotens: a már kitöltött sorokhoz nem nyúl.

**Előfeltétel:** ez a task a Task 4 migrációjára épül, ami még nincs kiadva. A munka megkezdése előtt kösd be a csomagot path repositoryként (lásd a „Lokális fejlesztés a csomagon" szakaszt), majd futtasd: `php artisan migrate`. A subscriber nem kap saját migrációt — a `loadMigrationsFrom()` miatt a csomagé itt is lefut.

- [ ] **Step 1: Írd meg a bukó tesztet**

Hozd létre: `tests/Feature/Console/BackfillIdentityUuidsCommandTest.php`

```php
<?php

declare(strict_types=1);

use App\Models\Team;
use App\Models\User;
use Illuminate\Support\Facades\DB;

use function Pest\Laravel\artisan;

it('fills the uuid on users and teams that have none', function (): void {
    $user = User::factory()->create();
    $team = Team::query()->create(['name' => 'Acme', 'slug' => 'acme']);

    DB::table('users')->where('id', $user->id)->update(['uuid' => null]);
    DB::table('teams')->where('id', $team->id)->update(['uuid' => null]);

    artisan('identity:backfill-uuids')->assertExitCode(0);

    expect($user->refresh()->uuid)->not->toBeNull()
        ->and($team->refresh()->uuid)->not->toBeNull();
});

it('leaves an existing uuid untouched', function (): void {
    $team = Team::query()->create(['name' => 'Acme', 'slug' => 'acme']);
    $original = $team->refresh()->uuid;

    expect($original)->not->toBeNull();

    artisan('identity:backfill-uuids')->assertExitCode(0);

    expect($team->refresh()->uuid)->toBe($original);
});

it('assigns a uuid automatically to newly created records', function (): void {
    $team = Team::query()->create(['name' => 'Fresh', 'slug' => 'fresh']);
    $user = User::factory()->create();

    expect($team->uuid)->not->toBeNull()
        ->and($user->uuid)->not->toBeNull();
});

it('gives every record a distinct uuid', function (): void {
    Team::query()->create(['name' => 'One', 'slug' => 'one']);
    Team::query()->create(['name' => 'Two', 'slug' => 'two']);

    expect(Team::query()->distinct()->count('uuid'))->toBe(2);
});
```

- [ ] **Step 2: Futtasd, győződj meg róla, hogy bukik**

Run: `cd /Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1 && vendor/bin/pest tests/Feature/Console/BackfillIdentityUuidsCommandTest.php`
Expected: FAIL — nincs `uuid` oszlop

- [ ] **Step 3: Futtasd a Task 4 migrációját a subscriberben**

```bash
cd /Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1
composer config repositories.local-uts path /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid
composer require madbox-99/laravel-user-team-sync:@dev --no-interaction
php artisan migrate
```

Ellenőrzés: `php artisan tinker --execute="dump(Schema::hasColumn('teams', 'uuid'));"` → `true`

- [ ] **Step 4: Vedd fel a `uuid`-t a modellekbe és generáld automatikusan**

Módosítsd: `app/Models/User.php` — a `#[Fillable([...])]` attribútum listájának **elejére** vedd fel a `'uuid',` elemet.

Ugyanebben a fájlban add hozzá a `booted` hookot a `casts()` metódus fölé:

```php
    protected static function booted(): void
    {
        static::creating(function (self $user): void {
            $user->uuid ??= (string) Str::uuid();
        });
    }
```

Add hozzá: `use Illuminate\Support\Str;`

Módosítsd: `app/Models/Team.php` — a subscriber Team a csomag Team-jéből származik, ami a `uuid`-t már fillable-ként kezeli (Task 4). Csak a generálást kell hozzáadni:

```php
    protected static function booted(): void
    {
        static::creating(function (self $team): void {
            $team->uuid ??= (string) Str::uuid();
        });
    }
```

Add hozzá: `use Illuminate\Support\Str;`

- [ ] **Step 5: Írd meg a parancsot**

Hozd létre: `app/Console/Commands/BackfillIdentityUuidsCommand.php`

```php
<?php

declare(strict_types=1);

namespace App\Console\Commands;

use App\Models\Team;
use App\Models\User;
use Illuminate\Console\Attributes\Description;
use Illuminate\Console\Attributes\Signature;
use Illuminate\Console\Command;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Support\Str;

/**
 * Assigns a UUID to every pre-existing user and team on this app.
 *
 * New records get theirs from the model's creating hook; this only covers rows
 * that predate the uuid column. Idempotent — rows that already have one are
 * skipped, so it is safe to re-run.
 */
#[Signature('identity:backfill-uuids {--dry-run : Report what would change without writing}')]
#[Description('Assign a UUID to every user and team that does not have one yet.')]
final class BackfillIdentityUuidsCommand extends Command
{
    public function handle(): int
    {
        $dryRun = (bool) $this->option('dry-run');

        if ($dryRun) {
            $this->warn('DRY-RUN mode — no changes will be persisted.');
        }

        $total = 0;

        foreach (['users' => User::class, 'teams' => Team::class] as $label => $modelClass) {
            /** @var class-string<Model> $modelClass */
            $query = $modelClass::query()->whereNull('uuid');
            $count = (clone $query)->count();

            $this->line(sprintf('%s: %d row(s) without a uuid.', $label, $count));

            if ($count === 0 || $dryRun) {
                $total += $count;

                continue;
            }

            $query->each(function (Model $model): void {
                $model->forceFill(['uuid' => (string) Str::uuid()])->saveQuietly();
            });

            $total += $count;
        }

        $this->newLine();
        $this->info(sprintf('%s %d row(s).', $dryRun ? 'Would update' : 'Updated', $total));

        return self::SUCCESS;
    }
}
```

- [ ] **Step 6: Futtasd a tesztet**

Run: `vendor/bin/pest tests/Feature/Console/BackfillIdentityUuidsCommandTest.php`
Expected: PASS (4 teszt)

- [ ] **Step 7: Futtasd a teljes tesztkészletet**

Run: `vendor/bin/pest`
Expected: PASS. Ha a `UserFactory` vagy `TeamFactory` explicit `uuid`-t vár, igazítsd — de a `creating` hook miatt nem kellene.

- [ ] **Step 8: Pint és commit**

A `composer.json` / `composer.lock` a path repository miatt piszkos marad — **ne add hozzá a committhoz**. A Task 7 Step 5-ben áll vissza, amikor a v1.11.0 már kiadott.

```bash
cd /Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1
vendor/bin/pint --dirty
git add app/Models/User.php app/Models/Team.php app/Console/Commands/BackfillIdentityUuidsCommand.php tests/Feature/Console/BackfillIdentityUuidsCommandTest.php
git commit -m "feat: uuid generálás és backfill a subscriberben

Minden új user és team születésekor uuid-t kap; a korábbi sorokat az
identity:backfill-uuids tölti fel. Idempotens, újrafuttatható."
```

---

## Task 6: A receiver fogadja és eltárolja a `uuid`-t

Két úton kell megérkeznie a uuid-nek a modul-appokra: az **új** entitásoknál a meglévő `create-user` / `create-team` payloadban, a **meglévő** soroknál pedig egy tömeges leképezés-végponton.

**Files:**
- Create: `src/Receiver/Http/Controllers/IdentityUuidController.php`
- Create: `src/Receiver/Http/Requests/StoreIdentityUuidsRequest.php`
- Modify: `src/Receiver/Http/Requests/CreateUserRequest.php`
- Modify: `src/Receiver/Http/Requests/CreateTeamRequest.php`
- Modify: `src/Receiver/Http/Controllers/UserSyncController.php:36-44`
- Modify: `src/Receiver/Http/Controllers/TeamSyncController.php:33-36`
- Modify: `routes/sync-api.php`
- Test: `tests/Feature/Receiver/IdentityUuidControllerTest.php`
- Test: `tests/Feature/Receiver/UserSyncControllerTest.php` (bővítés)
- Test: `tests/Feature/Receiver/TeamSyncControllerTest.php` (bővítés)

**Interfaces:**
- Produces: `POST /api/identity-uuids` (Bearer auth), kérés:
  ```
  {
    "users": [{"email": string, "uuid": string}],
    "teams": [{"slug": string,  "uuid": string}]
  }
  ```
  válasz: `{"users_updated": int, "teams_updated": int, "users_missing": [string], "teams_missing": [string]}`
- Produces: `create-user` és `create-team` opcionális `uuid` mezőt fogad.

- [ ] **Step 1: Írd meg a bukó tesztet a tömeges végpontra**

Hozd létre: `tests/Feature/Receiver/IdentityUuidControllerTest.php`

```php
<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Hash;
use Illuminate\Support\Str;
use Madbox99\UserTeamSync\Tests\Fixtures\Team;
use Madbox99\UserTeamSync\Tests\Fixtures\User;

it('rejects requests without a valid api key', function (): void {
    $this->postJson('/api/identity-uuids', [])->assertStatus(401);
});

it('writes uuids onto existing users and teams', function (): void {
    $user = User::create([
        'name' => 'User',
        'email' => 'user@example.com',
        'password' => Hash::make('pass'),
    ]);
    $team = Team::create(['name' => 'Acme', 'slug' => 'acme']);

    $userUuid = (string) Str::uuid();
    $teamUuid = (string) Str::uuid();

    $this->postJson('/api/identity-uuids', [
        'users' => [['email' => 'user@example.com', 'uuid' => $userUuid]],
        'teams' => [['slug' => 'acme', 'uuid' => $teamUuid]],
    ], authHeaders())
        ->assertOk()
        ->assertJson(['users_updated' => 1, 'teams_updated' => 1]);

    expect($user->refresh()->uuid)->toBe($userUuid)
        ->and($team->refresh()->uuid)->toBe($teamUuid);
});

it('reports records it could not find instead of failing', function (): void {
    $response = $this->postJson('/api/identity-uuids', [
        'users' => [['email' => 'ghost@example.com', 'uuid' => (string) Str::uuid()]],
        'teams' => [['slug' => 'no-such-team', 'uuid' => (string) Str::uuid()]],
    ], authHeaders())->assertOk();

    expect($response->json('users_updated'))->toBe(0)
        ->and($response->json('users_missing'))->toBe(['ghost@example.com'])
        ->and($response->json('teams_missing'))->toBe(['no-such-team']);
});

it('is idempotent', function (): void {
    Team::create(['name' => 'Acme', 'slug' => 'acme']);
    $uuid = (string) Str::uuid();

    $payload = ['users' => [], 'teams' => [['slug' => 'acme', 'uuid' => $uuid]]];

    $this->postJson('/api/identity-uuids', $payload, authHeaders())->assertOk();
    $this->postJson('/api/identity-uuids', $payload, authHeaders())->assertOk();

    expect(Team::where('slug', 'acme')->first()->uuid)->toBe($uuid);
});

it('does not overwrite a uuid that is already set to a different value', function (): void {
    $existing = (string) Str::uuid();
    Team::create(['name' => 'Acme', 'slug' => 'acme', 'uuid' => $existing]);

    $response = $this->postJson('/api/identity-uuids', [
        'users' => [],
        'teams' => [['slug' => 'acme', 'uuid' => (string) Str::uuid()]],
    ], authHeaders())->assertOk();

    expect(Team::where('slug', 'acme')->first()->uuid)->toBe($existing)
        ->and($response->json('teams_conflicting'))->toBe(['acme']);
});
```

- [ ] **Step 2: Futtasd, győződj meg róla, hogy bukik**

Run: `cd /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid && vendor/bin/pest tests/Feature/Receiver/IdentityUuidControllerTest.php`
Expected: FAIL — 404

- [ ] **Step 3: Írd meg a request osztályt**

Hozd létre: `src/Receiver/Http/Requests/StoreIdentityUuidsRequest.php`

```php
<?php

declare(strict_types=1);

namespace Madbox99\UserTeamSync\Receiver\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

final class StoreIdentityUuidsRequest extends FormRequest
{
    public function authorize(): bool
    {
        return true;
    }

    /**
     * @return array<string, array<int, string>>
     */
    public function rules(): array
    {
        return [
            'users' => ['present', 'array'],
            'users.*.email' => ['required', 'email'],
            'users.*.uuid' => ['required', 'uuid'],
            'teams' => ['present', 'array'],
            'teams.*.slug' => ['required', 'string'],
            'teams.*.uuid' => ['required', 'uuid'],
        ];
    }
}
```

- [ ] **Step 4: Írd meg a controllert**

Hozd létre: `src/Receiver/Http/Controllers/IdentityUuidController.php`

```php
<?php

declare(strict_types=1);

namespace Madbox99\UserTeamSync\Receiver\Http\Controllers;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Http\JsonResponse;
use Illuminate\Routing\Controller;
use Madbox99\UserTeamSync\Receiver\Http\Requests\StoreIdentityUuidsRequest;

/**
 * Bulk-applies the publisher's UUID mapping to pre-existing rows on this app.
 *
 * Matches users by email and teams by slug — the only cross-app keys that
 * exist before UUIDs do. Never overwrites a UUID that is already set to a
 * different value: that means the two sides disagree about identity, which a
 * human has to resolve, so it is reported rather than silently corrected.
 */
final class IdentityUuidController extends Controller
{
    public function __invoke(StoreIdentityUuidsRequest $request): JsonResponse
    {
        $validated = $request->validated();

        /** @var class-string<Model> $userModel */
        $userModel = config('user-team-sync.models.user');

        /** @var class-string<Model> $teamModel */
        $teamModel = config('user-team-sync.models.team');

        $users = $this->apply($userModel, 'email', $validated['users']);
        $teams = $this->apply($teamModel, 'slug', $validated['teams']);

        return response()->json([
            'users_updated' => $users['updated'],
            'users_missing' => $users['missing'],
            'users_conflicting' => $users['conflicting'],
            'teams_updated' => $teams['updated'],
            'teams_missing' => $teams['missing'],
            'teams_conflicting' => $teams['conflicting'],
        ]);
    }

    /**
     * @param  class-string<Model>  $modelClass
     * @param  array<int, array{uuid: string, email?: string, slug?: string}>  $rows
     * @return array{updated: int, missing: array<int, string>, conflicting: array<int, string>}
     */
    private function apply(string $modelClass, string $matchColumn, array $rows): array
    {
        $updated = 0;
        $missing = [];
        $conflicting = [];

        foreach ($rows as $row) {
            $key = $row[$matchColumn];

            $record = $modelClass::query()->where($matchColumn, $key)->first();

            if (! $record instanceof Model) {
                $missing[] = $key;

                continue;
            }

            $current = $record->getAttribute('uuid');

            if ($current === $row['uuid']) {
                continue;
            }

            if ($current !== null) {
                $conflicting[] = $key;

                continue;
            }

            $record->forceFill(['uuid' => $row['uuid']])->saveQuietly();
            $updated++;
        }

        return ['updated' => $updated, 'missing' => $missing, 'conflicting' => $conflicting];
    }
}
```

- [ ] **Step 5: Vedd fel a route-ot**

Módosítsd: `routes/sync-api.php` — import és route a group-on belül:

```php
use Madbox99\UserTeamSync\Receiver\Http\Controllers\IdentityUuidController;
```

```php
        Route::post('/identity-uuids', IdentityUuidController::class);
```

- [ ] **Step 6: Futtasd a tesztet**

Run: `vendor/bin/pest tests/Feature/Receiver/IdentityUuidControllerTest.php`
Expected: PASS (5 teszt)

- [ ] **Step 7: Írd meg a bukó teszteket az új entitások uuid-jére**

Fűzd hozzá a `tests/Feature/Receiver/TeamSyncControllerTest.php` fájl végéhez:

```php
it('stores the uuid supplied with a new team', function (): void {
    $uuid = (string) Illuminate\Support\Str::uuid();

    $this->postJson('/api/create-team', [
        'name' => 'Uuid Team',
        'slug' => 'uuid-team',
        'uuid' => $uuid,
    ], authHeaders())->assertStatus(201);

    expect(Team::where('slug', 'uuid-team')->first()->uuid)->toBe($uuid);
});

it('still creates a team when no uuid is supplied', function (): void {
    $this->postJson('/api/create-team', [
        'name' => 'Legacy Team',
        'slug' => 'legacy-team',
    ], authHeaders())->assertStatus(201);

    expect(Team::where('slug', 'legacy-team')->first()->uuid)->toBeNull();
});
```

Fűzd hozzá a `tests/Feature/Receiver/UserSyncControllerTest.php` fájl végéhez:

```php
it('stores the uuid supplied with a new user', function (): void {
    $uuid = (string) Illuminate\Support\Str::uuid();

    $this->postJson('/api/create-user', [
        'email' => 'uuid-user@example.com',
        'name' => 'Uuid User',
        'password_hash' => Hash::make('secret'),
        'uuid' => $uuid,
    ], authHeaders())->assertStatus(201);

    expect(User::where('email', 'uuid-user@example.com')->first()->uuid)->toBe($uuid);
});

it('still creates a user when no uuid is supplied', function (): void {
    $this->postJson('/api/create-user', [
        'email' => 'legacy-user@example.com',
        'name' => 'Legacy User',
        'password_hash' => Hash::make('secret'),
    ], authHeaders())->assertStatus(201);

    expect(User::where('email', 'legacy-user@example.com')->first()->uuid)->toBeNull();
});
```

- [ ] **Step 8: Futtasd, győződj meg róla, hogy a uuid-s tesztek buknak**

Run: `vendor/bin/pest tests/Feature/Receiver --filter="uuid"`
Expected: a két „stores the uuid supplied" teszt FAIL (a uuid `null` marad), a két „still creates" teszt PASS

- [ ] **Step 9: Vedd fel az opcionális `uuid`-t a request osztályokba**

Módosítsd: `src/Receiver/Http/Requests/CreateUserRequest.php` — a `rules()` tömb elejére:

```php
            'uuid' => ['nullable', 'uuid', 'unique:users,uuid'],
```

Módosítsd: `src/Receiver/Http/Requests/CreateTeamRequest.php` — a `rules()` tömb elejére:

```php
            'uuid' => ['nullable', 'uuid', 'unique:teams,uuid'],
```

- [ ] **Step 10: Tárold el a uuid-t a controllerekben**

Módosítsd: `src/Receiver/Http/Controllers/UserSyncController.php` — a `create()` metódusban a `$userModel::query()->create([...])` hívás tömbjébe vedd fel első elemként:

```php
                'uuid' => $validated['uuid'] ?? null,
```

Módosítsd: `src/Receiver/Http/Controllers/TeamSyncController.php` — a `create()` metódusban a `$teamModel::query()->create([...])` hívás tömbjébe vedd fel első elemként:

```php
            'uuid' => $validated['uuid'] ?? null,
```

- [ ] **Step 11: Futtasd a receiver teszteket**

Run: `vendor/bin/pest tests/Feature/Receiver`
Expected: PASS, minden teszt

- [ ] **Step 12: Futtasd a teljes csomag-tesztkészletet**

Run: `vendor/bin/pest`
Expected: PASS

- [ ] **Step 13: Pint, commit és kiadás**

```bash
cd /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid
vendor/bin/pint --dirty
git add src routes tests
git commit -m "feat: a receiver fogadja és eltárolja a globális uuid-t

Új entitásoknál a create-user/create-team payloadban, a meglévő soroknál
a POST /api/identity-uuids tömeges leképezésen keresztül. Mindkettő
opcionális: uuid nélkül minden pontosan úgy működik, mint eddig."
```

> **Kiadás és deploy — NEM ennek a tasknak a része.** A `v1.11.0` tag, a push, és a `composer update` + `php artisan migrate` a 16 modul-appon a branch mergelése után történik.

---

## Task 7: A publisher küldi a `uuid`-t

**Files:**
- Modify: `src/Publisher/PublisherService.php:96-101` (`createUser`), `:118-121` (`createTeam`)
- Modify: `src/Publisher/Jobs/CreateUserJob.php`
- Modify: `src/Publisher/Jobs/CreateTeamJob.php`
- Modify: `app/Filament/Pages/Auth/Register.php` és `app/Actions/CreateCustomer.php` (subscriber — uuid átadása)
- Create: `app/Console/Commands/PushIdentityUuidsCommand.php` (subscriber)
- Test: `tests/Unit/Publisher/Jobs/CreateUserJobTest.php`, `CreateTeamJobTest.php` (csomag, bővítés)
- Test: `tests/Feature/Console/PushIdentityUuidsCommandTest.php` (subscriber)

**Interfaces:**
- Consumes: `POST /api/identity-uuids` (Task 6), `create-user` / `create-team` `uuid` mezője (Task 6)
- Produces:
  - `PublisherService::createUser(string $email, string $name, string $password, string $role, string $ownerEmail, ?string $uuid = null): void`
  - `PublisherService::createTeam(string $teamName, string $userEmail, ?string $slug = null, ?string $userName = null, ?string $uuid = null): void`
  - `php artisan identity:push-uuids` — a meglévő sorok leképezésének kiküldése

- [ ] **Step 1: Írd meg a bukó teszteket a jobokra**

Fűzd hozzá a `tests/Unit/Publisher/Jobs/CreateTeamJobTest.php` fájl végéhez:

```php
it('sends the uuid in the create-team payload', function (): void {
    Illuminate\Support\Facades\Http::fake([
        'https://crm.test/api/create-team' => Illuminate\Support\Facades\Http::response(['team_id' => 1], 201),
    ]);

    config()->set('user-team-sync.publisher.app_source', 'config');
    config()->set('user-team-sync.publisher.apps', [
        'crm' => ['url' => 'https://crm.test', 'api_key' => 'k', 'active' => true],
    ]);

    $uuid = (string) Illuminate\Support\Str::uuid();

    (new Madbox99\UserTeamSync\Publisher\Jobs\CreateTeamJob(
        teamName: 'Acme',
        userEmail: 'owner@example.com',
        slug: 'acme',
        userName: 'Owner',
        uuid: $uuid,
    ))->handle(app(Madbox99\UserTeamSync\Publisher\PublisherService::class));

    Illuminate\Support\Facades\Http::assertSent(
        fn ($request): bool => $request->url() === 'https://crm.test/api/create-team'
            && $request['uuid'] === $uuid
    );
});
```

Fűzd hozzá a `tests/Unit/Publisher/Jobs/CreateUserJobTest.php` fájl végéhez:

```php
it('sends the uuid in the create-user payload', function (): void {
    Illuminate\Support\Facades\Http::fake([
        'https://crm.test/api/user-teams' => Illuminate\Support\Facades\Http::response(['teams' => []]),
        'https://crm.test/api/create-user' => Illuminate\Support\Facades\Http::response(['user_id' => 1], 201),
    ]);

    config()->set('user-team-sync.publisher.app_source', 'config');
    config()->set('user-team-sync.publisher.apps', [
        'crm' => ['url' => 'https://crm.test', 'api_key' => 'k', 'active' => true],
    ]);

    $uuid = (string) Illuminate\Support\Str::uuid();

    (new Madbox99\UserTeamSync\Publisher\Jobs\CreateUserJob(
        email: 'new@example.com',
        name: 'New',
        passwordHash: Illuminate\Support\Facades\Hash::make('secret'),
        role: 'subscriber',
        ownerEmail: 'owner@example.com',
        uuid: $uuid,
    ))->handle(app(Madbox99\UserTeamSync\Publisher\PublisherService::class));

    Illuminate\Support\Facades\Http::assertSent(
        fn ($request): bool => $request->url() === 'https://crm.test/api/create-user'
            && $request['uuid'] === $uuid
    );
});
```

- [ ] **Step 2: Futtasd, győződj meg róla, hogy bukik**

Run: `cd /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid && vendor/bin/pest tests/Unit/Publisher/Jobs --filter="uuid"`
Expected: FAIL — `Unknown named parameter $uuid`

- [ ] **Step 3: Vedd fel a `uuid` paramétert a jobokba és a service-be**

Módosítsd: `src/Publisher/Jobs/CreateUserJob.php` — a konstruktor utolsó paramétereként:

```php
        public readonly ?string $uuid = null,
```

és a `$http->post("{$app['url']}/api/create-user", [...])` payload tömbjébe első elemként:

```php
                    'uuid' => $this->uuid,
```

Módosítsd: `src/Publisher/Jobs/CreateTeamJob.php` — a konstruktor utolsó paramétereként ugyanígy `public readonly ?string $uuid = null,`, és a `create-team` payloadba `'uuid' => $this->uuid,`.

Módosítsd: `src/Publisher/PublisherService.php`:

```php
    public function createUser(string $email, string $name, string $password, string $role, string $ownerEmail, ?string $uuid = null): void
    {
        $passwordHash = Hash::isHashed($password) ? $password : Hash::make($password);

        $this->dispatchJob(new CreateUserJob($email, $name, $passwordHash, $role, $ownerEmail, $uuid));
    }
```

```php
    public function createTeam(string $teamName, string $userEmail, ?string $slug = null, ?string $userName = null, ?string $uuid = null): void
    {
        $this->dispatchJob(new CreateTeamJob($teamName, $userEmail, $slug, $userName, $uuid));
    }
```

**Figyelem:** a `CreateTeamJob` konstruktorában a `$uuid` a `$userName` **után** jön, hogy a meglévő pozicionális hívások ne törjenek el.

- [ ] **Step 4: Futtasd a csomag-teszteket**

Run: `vendor/bin/pest`
Expected: PASS, minden teszt

- [ ] **Step 5: Pint, commit és kiadás**

```bash
cd /Users/szabozoltan/Herd/laravel-user-team-sync/.worktrees/identity-uuid
vendor/bin/pint --dirty
git add src tests
git commit -m "feat: a publisher küldi a globális uuid-t a create payloadokban

Opcionális paraméter a jobok végén, hogy a meglévő pozicionális hívások
változatlanul működjenek."
```

> **Kiadás — NEM ennek a tasknak a része.** A `v1.12.0` tag és a push a branch mergelése után történik. A path repository bekötve marad, hogy a Task 7 további lépései a friss csomagkódot lássák.

- [ ] **Step 6: Add át a uuid-t a subscriber hívási pontjain**

Módosítsd: `app/Filament/Pages/Auth/Register.php` — a `handleRegistration()` végén lévő két hívás:

```php
        UserTeamSync::createUser(
            email: $user->email,
            name: $user->name,
            password: $this->rawPassword,
            role: $user->role?->value ?? UserRole::Manager->value,
            ownerEmail: $user->email,
            uuid: $user->uuid,
        );

        UserTeamSync::createTeam(
            teamName: $user->company_name,
            userEmail: $user->email,
            slug: $team->slug,
            userName: $user->name,
            uuid: $team->uuid,
        );
```

Módosítsd: `app/Actions/CreateCustomer.php` — a `provisionAcrossApps()` metódusban ugyanígy add hozzá az `uuid: $owner->uuid,` és `uuid: $team->uuid,` argumentumokat.

- [ ] **Step 7: Írd meg a bukó tesztet a push parancsra**

Hozd létre: `tests/Feature/Console/PushIdentityUuidsCommandTest.php`

```php
<?php

declare(strict_types=1);

use App\Models\Team;
use App\Models\User;
use Illuminate\Support\Facades\Http;
use Madbox99\UserTeamSync\Models\SyncApp;

use function Pest\Laravel\artisan;

beforeEach(function (): void {
    config()->set('user-team-sync.publisher.app_source', 'database');

    SyncApp::create([
        'name' => 'crm',
        'url' => 'https://crm.test',
        'api_key' => 'secret',
        'is_active' => true,
    ]);
});

it('sends every user and team uuid to each active app', function (): void {
    $user = User::factory()->create(['email' => 'owner@example.com']);
    $team = Team::query()->create(['name' => 'Acme', 'slug' => 'acme']);

    Http::fake([
        'https://crm.test/api/identity-uuids' => Http::response([
            'users_updated' => 1, 'users_missing' => [], 'users_conflicting' => [],
            'teams_updated' => 1, 'teams_missing' => [], 'teams_conflicting' => [],
        ]),
    ]);

    artisan('identity:push-uuids')->assertExitCode(0);

    Http::assertSent(function ($request) use ($user, $team): bool {
        return $request->url() === 'https://crm.test/api/identity-uuids'
            && $request['users'][0] === ['email' => $user->email, 'uuid' => $user->uuid]
            && $request['teams'][0] === ['slug' => $team->slug, 'uuid' => $team->uuid];
    });
});

it('reports conflicts loudly and exits non-zero', function (): void {
    User::factory()->create();

    Http::fake([
        'https://crm.test/api/identity-uuids' => Http::response([
            'users_updated' => 0, 'users_missing' => [], 'users_conflicting' => ['owner@example.com'],
            'teams_updated' => 0, 'teams_missing' => [], 'teams_conflicting' => [],
        ]),
    ]);

    artisan('identity:push-uuids')
        ->expectsOutputToContain('conflict')
        ->assertExitCode(1);
});

it('skips records that have no uuid yet', function (): void {
    $user = User::factory()->create();
    Illuminate\Support\Facades\DB::table('users')->where('id', $user->id)->update(['uuid' => null]);

    Http::fake([
        'https://crm.test/api/identity-uuids' => Http::response([
            'users_updated' => 0, 'users_missing' => [], 'users_conflicting' => [],
            'teams_updated' => 0, 'teams_missing' => [], 'teams_conflicting' => [],
        ]),
    ]);

    artisan('identity:push-uuids')->assertExitCode(0);

    Http::assertSent(fn ($request): bool => $request['users'] === []);
});
```

- [ ] **Step 8: Futtasd, győződj meg róla, hogy bukik**

Run: `cd /Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1 && vendor/bin/pest tests/Feature/Console/PushIdentityUuidsCommandTest.php`
Expected: FAIL — `The command "identity:push-uuids" does not exist.`

- [ ] **Step 9: Írd meg a parancsot**

Hozd létre: `app/Console/Commands/PushIdentityUuidsCommand.php`

```php
<?php

declare(strict_types=1);

namespace App\Console\Commands;

use App\Models\Team;
use App\Models\User;
use Illuminate\Console\Attributes\Description;
use Illuminate\Console\Attributes\Signature;
use Illuminate\Console\Command;
use Madbox99\UserTeamSync\Publisher\PublisherService;
use Throwable;

/**
 * Pushes this app's UUID mapping to every active module app, so pre-existing
 * rows there gain the same global identifier.
 *
 * Records the receiver could not match (missing) are informational — they were
 * already surfaced by identity:audit. Records where the receiver holds a
 * DIFFERENT uuid (conflicting) are a hard error: the two sides disagree about
 * identity and a human must resolve it before the SSO rollout.
 */
#[Signature('identity:push-uuids {--app= : Limit the push to a single app key}')]
#[Description('Send the user and team UUID mapping to every module app.')]
final class PushIdentityUuidsCommand extends Command
{
    public function handle(PublisherService $publisher): int
    {
        $apps = $publisher->getActiveApps();

        if ($only = $this->option('app')) {
            $apps = array_intersect_key($apps, [$only => true]);
        }

        if ($apps === []) {
            $this->warn('No active apps configured. Nothing to push.');

            return self::SUCCESS;
        }

        $users = User::query()
            ->whereNotNull('uuid')
            ->orderBy('id')
            ->get(['email', 'uuid'])
            ->map(fn (User $user): array => ['email' => $user->email, 'uuid' => $user->uuid])
            ->values()
            ->all();

        $teams = Team::query()
            ->whereNotNull('uuid')
            ->orderBy('id')
            ->get(['slug', 'uuid'])
            ->map(fn (Team $team): array => ['slug' => $team->slug, 'uuid' => $team->uuid])
            ->values()
            ->all();

        $this->info(sprintf('Pushing %d user(s) and %d team(s) to %d app(s).', count($users), count($teams), count($apps)));

        $hasConflict = false;

        foreach ($apps as $appName => $app) {
            $this->newLine();
            $this->info("=== {$appName} ===");

            try {
                $response = $publisher->makeHttpClient($app)
                    ->post("{$app['url']}/api/identity-uuids", ['users' => $users, 'teams' => $teams]);
            } catch (Throwable $throwable) {
                $this->error("  Unreachable: {$throwable->getMessage()}");
                $hasConflict = true;

                continue;
            }

            if (! $response->successful()) {
                $this->error("  HTTP {$response->status()}: {$response->body()}");
                $hasConflict = true;

                continue;
            }

            $body = $response->json();

            $this->line(sprintf('  Updated %d user(s), %d team(s).', $body['users_updated'], $body['teams_updated']));

            foreach (['users' => 'user', 'teams' => 'team'] as $group => $label) {
                $missing = $body["{$group}_missing"];

                if ($missing !== []) {
                    $this->warn(sprintf('  %d %s(s) not found on this app (see identity:audit).', count($missing), $label));
                }

                $conflicting = $body["{$group}_conflicting"];

                if ($conflicting !== []) {
                    $hasConflict = true;
                    $this->error(sprintf('  %d %s uuid conflict(s) — resolve by hand:', count($conflicting), $label));

                    foreach ($conflicting as $value) {
                        $this->line("      - {$value}");
                    }
                }
            }
        }

        $this->newLine();

        if ($hasConflict) {
            $this->error('Finished with conflicts or unreachable apps. Do NOT proceed to phase 2 until these are resolved.');

            return self::FAILURE;
        }

        $this->info('All apps updated cleanly.');

        return self::SUCCESS;
    }
}
```

- [ ] **Step 10: Futtasd a tesztet**

Run: `vendor/bin/pest tests/Feature/Console/PushIdentityUuidsCommandTest.php`
Expected: PASS (3 teszt)

- [ ] **Step 11: Futtasd a teljes tesztkészletet**

Run: `vendor/bin/pest`
Expected: PASS

- [ ] **Step 12: Pint és commit**

```bash
cd /Users/szabozoltan/Herd/subscriber/.worktrees/sso-phase-0-1
vendor/bin/pint --dirty
git add app tests composer.json composer.lock
git commit -m "feat: uuid átadása a szinkron hívásoknál és identity:push-uuids parancs

Az új entitások már születésükkor kapnak uuid-t minden appon; a meglévő
sorok leképezését az identity:push-uuids küldi ki. Ütközés esetén a
parancs nem nulla kóddal áll le."
```

> **Éles futtatás — NEM ennek a tasknak a része.** A `identity:backfill-uuids` és a `identity:push-uuids` valódi adatot ír és hálózaton hív modul-appokat. Az implementer a Step 12 committal végez.
>
> Merge és deploy után, emberi felügyelettel, ebben a sorrendben:
>
> ```bash
> php artisan identity:backfill-uuids --dry-run
> php artisan identity:backfill-uuids
> php artisan identity:push-uuids
> php artisan identity:audit
> ```
>
> **Az 1. fázis akkor kész, ha a `identity:push-uuids` nulla kóddal áll le** — tehát nincs sem ütközés, sem elérhetetlen app. Amíg nem, a 2. fázis (Passport IdP) nem kezdődhet el.

---

## Önellenőrzés — mit fed le ez a terv a specből

| Spec elem | Task |
|---|---|
| 0. fázis: slug-ütközés javítása | Task 1 |
| 0. fázis: `identity:audit`, kézi rendezés | Task 2, Task 3 |
| 1. fázis: `uuid` oszlop mind a 17 appban | Task 4 (16 modul-app), Task 5 (subscriber) |
| 1. fázis: backfill email/slug párosítással | Task 5 (lokális), Task 6 + 7 (kiküldés) |
| 1. fázis: új entitások születésükkor kapnak uuid-t | Task 5 (`creating` hook), Task 6, Task 7 |
| 1. fázis: „ha itt megállnánk, semmi nem romlana el" | Minden csomag-változtatás `nullable` / opcionális; Task 4 és 6 tesztjei kifejezetten ellenőrzik a uuid nélküli utat |

**Ami szándékosan NEM része ennek a tervnek** (a 2-5. fázisé): Passport telepítése, `/api/userinfo`, `IdentityProvisioner`, a `client` mód, a `sessions` tábla és queue átállítása Forge-on, a `tenantRegistration` kivezetése, a revoke végpont. Ezekre a 0. fázis auditjának eredménye után készül külön terv.

---

## Merge és deploy — kötelező sorrend

Ez a terv szándékosan nem tartalmaz release-lépéseket az implementer feladatai között — feature branch-ről nem megyünk tag-elésbe vagy push-ba. Ez a szakasz nem egy újabb taskot ad hozzá a implementer scope-jához, hanem **explicit gazdát rendel** ahhoz a sorrendhez, ami eddig csak prózában létezett, és amit semmi nem kényszerített ki. Ez a hiány okozta a végső review Fix 1-es kritikus hibáját: a commitolt `composer.lock` v1.9.0-ra van pinnelve, miközben a commitolt alkalmazáskód már a csomag `feat/identity-uuid` branch-ének (leendő v1.11.0) uuid-es API-ját hívja.

Az alábbi lépéssor **kötelező és sorrendhez kötött**. Felelős: a release-t végző fejlesztő/DevOps (nem az implementer).

- [ ] **1. Mindkét branch merge-elése.** `feat/identity-uuid` a csomagban (`laravel-user-team-sync`), `feat/sso-phase-0-1` a subscriberben.
- [ ] **2. A csomag release tag-elése és push-olása.** A release-nek tartalmaznia kell a Task 2, 4, 6 és 7 alatti csomag-oldali munkát. Verzió: `v1.11.0` vagy újabb.
- [ ] **3. A 16 modul-app frissítése.** Mindegyiken: `composer update madbox-99/laravel-user-team-sync` majd `php artisan migrate`.
- [ ] **4. Csak ezután: a subscriber leválasztása a path repository-ról.** A `local-uts` path repository bejegyzés eltávolítása a `composer.json`-ból, `composer update madbox-99/laravel-user-team-sync` a kiadott verzióra, `php artisan migrate`, majd a teljes tesztkészlet futtatása a valódi (nem path) függőség ellen.
- [ ] **5. Csak ezután: a subscriber deploy-olása.**
- [ ] **6. Csak ezután, felügyelt éles futtatással, ebben a sorrendben:**
  ```bash
  php artisan identity:backfill-uuids --dry-run
  php artisan identity:backfill-uuids
  php artisan identity:push-uuids
  php artisan identity:audit
  ```

**Miért `^1.11` a commitolt constraint, miközben a commitolt lock v1.9.0-nál régebbi marad?** Pontosan a fenti 2–4. lépések miatt. A 2–4. lépés lezárásáig nincs kiadott csomagverzió, amivel a lock frissíthető lenne — a `composer.json` constraint-jét mégis előre `^1.11`-re emeltük (lásd Fix 1 a végső review-ban), hogy egy friss klónon a `composer install` **azonnal, lock-out-of-sync hibával álljon le**, ahelyett hogy egy futásidejű `Unknown named parameter $uuid` vagy `Unknown column 'uuid'` hibával bukjon el production-ben. Ez a fail-fast viselkedés **szándékos, nem hiba** — addig marad így, amíg a 4. lépés le nem zajlik, és a path repository ki nem kerül a `composer.json`-ból.
